// import React from 'react'; // Not sure if this is needed

/*
	(c) 2016. All rights reserved.
	The music chatboard..!
*/

let Music_Chatboard = (chatboard_data) => {
	<div>
		{chatboard_data.lines.map(
			one_line =>
			<div>Yo brah</div>
		)}
	</div>
};

/* ---- Only export the top level React class ---- */
export { Music_Chatboard as default };
